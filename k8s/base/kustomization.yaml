apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  # NB: HPA + cert-manager Certificate live OUT-OF-BAND (not whitelisted in
  # the applications AppProject). Platform team applies them separately.

commonLabels:
  app.kubernetes.io/name: critica
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: inception-point
