apiVersion: v1
kind: Service
metadata:
  name: critica
  labels:
    app: critica
spec:
  type: ClusterIP
  selector:
    app: critica
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
